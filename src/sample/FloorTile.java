package sample;

public class FloorTile extends Tile {
    // Constructors
    public FloorTile(Room inRoom, double initialX, double initialY) {
        super(inRoom, initialX, initialY, "spr_dungeon_tile.png");
    }
}
